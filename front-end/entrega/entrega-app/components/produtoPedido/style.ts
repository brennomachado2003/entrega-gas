import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 8,
  },

  product: {
    color: "#F8FAFC",
    fontSize: 15,
    fontWeight: "600",
    letterSpacing: 0.4,
    marginBottom: 4,
  },

  loading: {
    marginTop: 8,
  },

  error: {
    color: "#EF4444",
    fontSize: 14,
    fontWeight: "500",
    letterSpacing: 0.4,
    marginTop: 8,
  },
});